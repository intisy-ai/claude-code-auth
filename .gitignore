node_modules/
dist/
# TeaVM-compiled orchestrator ESM staged by core/teavm-build.mjs at build time, never committed.
# The TeaVM bundle is build output; the .d.ts emitted beside it from the same Java IS committed,
# so the TypeScript seam type-checks on a clean clone before anything is built.
src/generated/claude-orchestrator.teavm.js
.DS_Store
.env
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.idea/
package-lock.json
.superpowers/

build/
*/build/
.gradle/
docs/
graphify-out/

# Generated on the default branch by the release readme job.
README.md
